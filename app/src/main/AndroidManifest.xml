<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.marcosoft.proyectofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PrincipalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".PrincipalActivity2"/>
        <activity android:name=".RegistroActivity" />
        <activity android:name=".Reportar1Activity" />
        <activity android:name=".Reportar2Activity" />
        <activity android:name=".Reportar3Activity"></activity>
    </application>

</manifest>